{
  "description": "Selection of one or more exact application-supplied NPC renderings.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "response_kind": {
      "type": "string",
      "enum": [
        "answer",
        "acknowledge",
        "refuse",
        "deflect",
        "ask_clarification",
        "react"
      ]
    },
    "rendering_ids": {
      "type": "array",
      "description": "Ordered distinct IDs of exact application-supplied rendering records. Application code concatenates their immutable text and derives all disclosure, outcome, episode, entity, and actor grounding from those records.",
      "items": {
        "type": "string"
      }
    }
  },
  "required": [
    "response_kind",
    "rendering_ids"
  ]
}
