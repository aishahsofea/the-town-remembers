# Generated or vendored output
node_modules/
dist/
dist-types/
build/
coverage/
cdk.out/
playwright-report/
test-results/
pnpm-lock.yaml

# Accepted decision records, their contract snapshots, and the phase plans are
# authored artifacts. The formatter governs code and configuration only, so a
# style change can never rewrite an accepted contract.
*.md
docs/

# Generated by `pnpm db:types` from the schema snapshot. The drift test compares
# the committed file byte for byte against what the generator produces, so the
# formatter must not rewrite it.
packages/database/src/schema.ts
